此项目是中间服务项目，目的是接收多个网站的数据，然后同步到CRM系统，此项目应具有较好的性能、持久化、日志记录、兼容性。各网站会一次性将需要同步的数据发送到此项目（需要考虑数据量大的情况的持久化问题）
# 数据同步中间服务方案总结

## 架构概述
多个网站 → HTTP API → 中间同步服务 → 消息队列 → CRM API → 状态查询

## 技术栈（使用较新的长时间维护的版本）
- **框架**: Spring Boot 3.4 + MyBatis + JDK21+Maven
- **消息队列**: Redis + Redisson
- **数据库**: MySQL 8
- **重试机制**: Spring Retry + 定时任务

## 核心功能

### 1. 数据库设计（不满足的话自行添加修改表）
```sql
CREATE TABLE sync_task (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    task_id VARCHAR(64) UNIQUE NOT NULL,
    website_code VARCHAR(32) NOT NULL,
    data_type VARCHAR(32) NOT NULL,
    business_id VARCHAR(64) NOT NULL,
    sync_data JSON NOT NULL,
    status TINYINT DEFAULT 0,
    retry_count INT DEFAULT 0,
    crm_request_code VARCHAR(64),
    error_msg TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### 2. 主要接口
- `POST /api/sync/submit` - 提交同步任务
- `GET /api/sync/status/{taskId}` - 查询同步状态

### 3. 处理流程
1. **接收请求** → 保存数据库 → 发送消息队列 → 返回taskId
2. **消费队列** → 调用CRM API → 保存requestCode → 更新状态
3. **定时查询** → 调用CRM结果查询接口 → 更新最终状态

### 4. 关键特性
- **去重**: 检查website_code+business_id是否已成功同步
- **重试**: 失败任务自动重试3次，间隔5秒
- **状态管理**: 0待处理、1处理中、2成功、3失败
- **异步处理**: CRM API异步执行，需要轮询结果

### 5. CRM API集成要点
- 单条数据提交，不支持批量，可能要列成队列，循坏请求
- 有时间间隔限制
- 重复数据会失败
- 返回requestCode后需要额外查询执行结果

### 6. 网站集成方式
各网站通过HTTP调用中间服务，一次性将所有数据发送给该项目，同时传入网站标识码区分来源。

**核心目标**: 简单、快速、可靠地实现多网站到CRM的数据同步，支持重试和状态跟踪。

CRM API接口文档如API-Ex文件所示，字段可能会变动。